# 项目知识学习

这是一份用于记录项目知识和学习过程的笔记。


## 整体简介

Java写业务

MySQL存数据

SpringMVC/SpringBoot写接口

maven管依赖和构建

git做协作

redis做缓存

Linux做部署和排错（线上服务器全是Linux，必须会用Linux来进行项目部署和项目后期维护）



## 1.JavaSE

（1）封装：set,get方法

（2）接口：定义规则，没有具体的，后续实现接口定义具体的

（3）异常机制

（4）Java负责写业务逻辑，MySQL负责存数据

## 2.MySQL

（1）金额用decimal（a,b），不用浮点数

（2）事务是为了保证数据一致性

（3）@Transactional表示这段业务要保证整体一致性

## 3.JavaWeb

（1）本质上就是浏览器发送请求，服务器接受请求，在返回响应

（2）get用来查数据，条件查询分页查询

​	post用来新增数据、提交表单、传复杂数据

（3）url是请求地址.比如：http://localhost:8080/user/list?page=1

（4）cookie：服务器让浏览器保存的一点标识信息.如果把账号密码放在cookie中是很不安全的

​	session：服务器保存的当前用户会话信息。浏览器访问服务器就是会话的开始。

​	把session id放在cookie中

**session保存在服务器那边。cookie id里放入session（不是把session放在cookie中。session就是存在服务器端的），运输到客户端那边。token诞生于服务器，但是保存在客户端** 

（5）springmvc偏向于解决web层请求分发和接口处理

##  4.SpringMVC

（1）把原来的servlet封装的更好用了。是为了帮我们更方便地**接收请求**、**处理参数**、**返回响应**的web框架

（2）controller是专门处理前端请求的入口类

（3）看到注解要明白这个类/方法在处理哪个请求

（4）`@RestController`表示返回json数据，而不是页面

（5）为什么返回json不返回jsp页面：做到前后端分离，后端主要提供数据，前端负责页面展示，所有用json更清晰、更通用

## 5.SpringBoot

（1）更快、更省事的把整个项目搭建起来

（2）springmvc主要解决请求响应，SpringBoot主要解决配置太麻烦、整合组建、项目启动、部署运行太麻烦等等问题

（3）配置文件用来放端口号、数据库连接、redis配置、日志配置、项目自定义配置

（4）entity是专门映射数据库的pojo

（5）config:配置（存放程序所有可变参数）

（6）依赖就是把别人写好的功能模块引进自己的项目

## 6.Maven

（1）maven不是写业务的，是**管理项目的**.主要管理依赖、构建、项目结构

（2）pom.xml理解为：这个项目的说明书+配置中心

（3）靠三部分标识：groupId:公司/组织/分组

​	artifactId:具体模块名

​	version:版本号

这三个合起来就是一个jar包的身份证

（4）starter:一套常用依赖的打包组合.不需要自己一个一个手动配

（5）clean:先把旧产物扫掉

​	compile：编译项目源代码

​	test：运行测试代码

​	package：把项目打包

​	install：除了打包，还会把包安装到本地maven仓库

## 7.Git

（1）git不是备份工具，是版本管理和团队协作工作

（2）merge是合并

（3）pull是把远程最新代码拉下来，和本地同步

（4）git add是把代码放入暂存区，git commit是把暂存区的代码提交到当前分支的本地仓库中

`add`：工作区 -> 暂存区

`commit`：暂存区 -> 本地仓库

`push`：本地仓库 -> 远程仓库

## 8.Redis

（1）redis本质上是一个**内存数据库**，最常见用途是**做缓存**（缓存：把经常用的数据临时放在一个更快的地方，下次用到直接拿）

（2）简单讲：每次查数据如果都去查mysql，导致MySQL压力大，并且查询慢，但是可以先查redis，这里吧数据放在内存中，并且快，redis是配合MySQL使用的

（3）验证码可以放在redis，token也可以存在redis中

（4）MySQL像正式档案库（存正式数据），redis更像是手边速查本（存热点、临时、高频数据）

## 9.Linux基础

（1）与部署、运行、查看、排错相关

（2）很多Java项目不是跑在本地的idea中，而是跑在Linux服务器上

（3）Linux是让项目在服务器上活起来

（4）Linux中一切都与路径有关

（5）cp 复制

​	mv 移动/重命名

​	rm 删除

​	mkdir 创建目录

（6）后端排错最常见的事情之一就是**看日志**

cat 查看文件全部内容

tail 查看文件末尾内容

（7）grep：在文件里找关键字（排错经常用到）

（8）`java -jar xxx.jar`是启动SpringBoot jar的核心形式

（9）端口是服务对外提供访问的入口之一

## 10.Codex

（1）询问的时候尽量问的详细一点，尤其是项目中大哥问题，这样才能更好的学习，收获更多，记得更牢固，要先自己分析一下

（2）你以后拿到陌生项目，可以这样让 Codex 帮你：

- 先帮我看这个项目入口在哪里
- 帮我梳理这个项目的目录结构
- 登录功能从前端请求到数据库查询经过了哪些类
- 这个模块的核心表有哪些
- 这个接口的执行链路是什么

（3）要把范问转化为带目标的问，要问的细节，不能太笼统

**高效问法：**

- 从项目角度讲 SpringBoot 里 `Controller/Service/Mapper` 怎么协作
- 如果 Redis 用来存验证码，完整流程是什么
- 带我看懂这个项目的用户登录模块
- 这段 SQL 我应该怎么拆开理解
- 这个报错优先排查哪三处

## 11.Agent

（1）rag:先检索外部知识，再把知识交给模型回答。重点在补知识

（2）agent:让模型具备任务拆解和工具调用能力。重点在会做事

## 12.HTTP、JSON、API 调用

（1）向一个服务发请求，再拿回结果：HTTP+JSON+API

（2）API：别人暴露给你调用的结构

理解：写SpringBoot的时候，你写controller接口，别人来调用，现在就是平台写好了接口，你直接用python去调用，这里接口就是API

（3）http就是通信协议

（4）json：跨语言传数据

（5）authorization：身份认证

==get和post都是http请求方法==

**区别：**

==get==一般用于查询数据，参数放在url(请求地址)里面

==post==一般用于提交复杂数据，参数放在请求体body里面（**模型API通常用POST**）



==API本质就是http+json，是程序与程序的接口，负责发送请求，拿回结果==







## 13.LLM（large language model）

 RAG:当你拿到一个特别大的文件，如果直接传，成本不可估计，用rag可以抽取用户问题最为匹配的几个片段发给大模型，让大模型根据这几个片段来回答问题

prompt（提示词）:大模型接受的具体问题或指令（通俗来讲就是你给大模型的指令）（接口契约）==(准确的说是提供给模型的输入上下文)==

system prompt：给大模型一个人设，告诉它之后提问他应该怎么做。也就是一个约束==控制模型行为==

user prompt：用户直接进行提问

tool：理解为函数。让大模型能够感知外部环境，提供这个能力

MCP（model context protool）：统一的接入规范。MCP作用理解为大部分手机都用type-C接口。MCP支持多种ai工具接入大模型，统一接口

 



==LLM本质上是一个根据上下文预测下一个token的模型==

LLM首先是在生成看起来最像合理回答的文本，所以有时候LLM会瞎说.**不是查资料，默认不知道最新外部资料，所以需要RAG。不是像人一样真正思考完再说==它是根据上下文预测==**



ai应用的很多错误，不是模型本身的问题，而是输入系统设计的问题（也就是说**出问题了首先反思一下输入系统设计**）->所以不能出问题了就怪罪模型，输入可能存在很多问题，要及时修正，找一下软件问题。要会拆是模型能力问题还是prompt问题还是rag检索问题还是上下文组织问题还是输出约束问题

==当你会反思问题出在哪里，而不是一味的依靠ai来生成，也是一个进步的过程，这样就不会过于依赖ai，一定不可以过于依赖ai，所以这次脱离codex时候一个很好的机会意识到自己之前出现的一些问题==

**之后对ai进行提问一定要问明确，比如问他从以下几个方面来给我解释一下这个名词等等，尽量避免泛泛的说给我解释一下这个名词（当你想详细了解的时候），不能说很多据单都是重复都是废话，要说的简洁但是内涵一定要包含全面**

**temperature**：控制输出随机性的参数（控制输出模型的随机性、创造性）这个值越低越保守，值越高越有创意越容易胡说。rag中推荐为0~0.2.**做项目一班用比较低的，不能太高**













## 14.RAG

rag是一套系统流程

rag包括：**文档采集**->**文档解析**->**文本切分**（chunk是切片的意思）->**向量化**（embedding是把“文本语义”编码成一串数字，语义相近的文本，在向量空间里距离更近）->**存入向量库**->**用户提问**->**问题向量化**->**相似度检索**->**上下文构造**->**LLM生成答案**

embedding模型是负责检索匹配，生成模型是负责回答

**RAG是召回率、准确率、成本、延迟之间的平衡**

query：查询语句/提问

**适合用rag**：知识来自外部资料、知识会更新、需要可控依据、不适合靠模型背下来

==RAG是为知识接入服务的==

**通俗理解**：embedding模型就是在向量化之后，把切片转成了向量，之后拿到问题embedding模型把问题向量化之后和库里的向量（数字）进行匹配

==RAG是一套工程思路，落脚点是一种思路==

**RAG解决的不是聊天问题，而是知识接入问题**

**RAG分为两个阶段**：离线阶段（把资料处理好，放进知识库）、在线阶段（用户提问时，查知识库，在让模型回答）

```
离线建库：
文档 -> 解析 -> 清洗 -> chunk -> embedding -> 向量库

在线问答：
问题 -> embedding -> 检索 top-k -> 拼上下文 -> LLM -> 答案
（在线问答对query进行embedding）
```

注意：**用户问题不用切分chunk，直接对用户问题整体embedding**

==chunk==**：知识库里参与检索的最小语义片段**

一个chunk入库设计为（例子）：

```
{
  "chunkId": "login_001",
  "content": "LoginController 接收 POST /login 请求，并调用 UserService.login。",
  "metadata": {
    "source": "LoginController.java",
    "module": "登录模块",
    "layer": "Controller",
    "path": "/login"
  }
}
```

**chunk切分过大会导致**：信息太混杂。匹配力度太粗。token成本更高。模型容易把controller、service、mapper、数据库混在一起说。后面引用sources时候不够准确。

**chunk切分太小会导致**：调用链路被切断。错误处理逻辑被拆散。模型拿到的片段可能缺少前因后果。top-k可能只召回零碎句子。

**选取最准确的top-k其实就是在召回率和噪声（准确率）之间进行调整**

只是向量检索不只是返回chunk，还会**返回一个相似度分数（score）**

**==真实的项目里先取top-k，再按照score阈值过滤，再拼接上下文==**

**RAG prompt举例：**

```
你是一个严谨的 Java 项目知识库助手。
你只能基于【项目资料】回答。
如果项目资料中没有依据，必须回答“知识库中未找到依据”，不要编造。

项目资料：
[chunk-B] UserService.login 如果用户不存在，返回用户名错误。
[chunk-C] UserService.login 如果密码不匹配，返回密码错误。

用户问题：
登录失败可能有哪些原因？

请返回 JSON：
{
  "answer": "",
  "sources": []
}

字段规则：
- answer：基于项目资料给出的回答。
- sources：数组，只能填写用到的 chunk 编号，例如 ["chunk-B"]。
- 如果没有依据，answer 写“知识库中未找到依据”，sources 返回空数组。
```

**RAG出错看三层**：

检索前：知识库有没有问题

检索中：召回的片段对不对

检索后：上下文和prompt有没有组织好





## 15.LangChain

是一个帮助组织LLM应用流程的工程框架

可以类比成springboot，这个不是数据库不是redis不是http本身，是帮忙把一堆组件组织起来的，平时常说springboot框架

**langchain也是一个框架**

主要学习他把哪些模块抽象出来了，为什么这样抽象

**model**：封装不同的大模型调用

**prompt template**：把prompt模板化

**output parser**（输出解释器）：把模型输出转为结构化格式（简单理解为把文本转为特定格式）

**retriever**：负责从知识库中检索相关文档。在embedding后面

**chain**:把多个步骤串起来

**agent**：让模型决定是否调用工具、调用哪个工具、调用几次

chain步骤固定，不灵活，agent自主决定调用哪个模型，流程不固定

 **agent（智能体）=LLM+planning(规划决策)+tools（工具）+memory（记忆）+action**













## 16.区分LLM和RAG

==LLM==本体不需要向量库，混淆了这两个。LLM是把文本切成token，然后通过模型参数计算下一个token的概率，选择一个token加入到上下文，再一个一个地生成答案

**完整流程：**

```
用户输入文本
-> tokenizer 切成 token
-> token 转成模型能处理的数字 ID
-> Transformer 根据上下文计算下一个 token 的概率
-> 选择一个 token
-> 把新 token 加回上下文
-> 继续预测下一个 token
-> 直到生成完整回答
```

==RAG==是外部系统（**rag里用到了embedding，LLM中没有用到**）

**完整流程：**

```
用户问题
-> 问题 embedding
-> 去向量库检索相关文档片段
-> 把检索片段 + 问题一起塞给 LLM
-> LLM 基于上下文生成答案
```

==LLM负责生成，RAG负责补知识，embedding是RAG检索用地，不是LLM回答问题必须经过地步骤==

```
LLM 适合：
公开知识、通用解释、代码思路、文本生成、总结、改写

RAG 适合：
私有文档、公司知识库、项目代码、最新资料、需要引用依据的回答

所以：如果询问一些公开的知识，比如问“请示Java中的接口是什么”，这种只用LLM是可以回答的，但是如果询问一些公司内部的文档等等，这些只用LLM会得到一份瞎说的答案
```



==LLM本身是有训练时得到的知识的，只是没有项目的私有知识，这些外部只是需要RAG，不是只要没有调用RAG就不能回答会瞎说。只调用LLM也可以回答一些公开的知识==



## 17.JSON

**JSON是一种语言传输结构化数据的文本格式**

JSON本质是文本，但是可以表示字符串、数字、数组、对象、布尔值

API返回json格式是因为程序好解析。



## 18.API请求框架

例如：

```
{
    "model":"chat-model",
    "messages":[
        {
            "role":"system",
            "content":"你要做一个“Java 项目知识库助手"
        },
        {
            "role":"user",
            "content":"这个项目的登录功能经过哪些层？"
        }
    ],
    "temperature":0.2
}
```



==如果后端没有给出message里面的content，也就是content为空，此时需要记录日志（简单理解就是出错啦），标记本次调用失败，然后在重试一次，如果还是失败就要返回统一错误结果给前端。==

统一错误结果比如：

```
{
  "success": false,
  "message": "模型暂时没有返回有效答案，请稍后重试"
}
```



## 19.prompt

prompt不是让模型更聪明，而是把输入设计的更像一份明确的接口契约

稳定prompt通用模板：角色、任务、信息来源、约束、输出格式、业务规则、用户问题

**示例：**

```
你是一个严谨的 Java 后端项目分析助手。

任务：
请根据【项目资料】分析登录流程。

信息来源限制：
你只能基于【项目资料】回答。
不要编造类名、方法名、接口名、数据库表名、字段名、SQL 或业务逻辑。
如果资料中没有某一层信息，必须写“资料中未提供”。

分析顺序：
请按 Controller -> Service -> Mapper -> 数据库 的顺序分析调用链路。

输出要求：
你必须只返回合法 JSON，不要返回 Markdown，不要返回代码块，不要返回额外解释文字。

JSON 格式必须严格符合：
{
  "answer": "对登录流程的整体总结",
  "steps": [
    {
      "layer": "Controller",
      "description": "这一层做了什么",
      "evidence": "来自项目资料的依据；如果没有依据，写资料中未提供"
    }
  ],
  "missingInfo": ["资料中缺失的信息"],
  "confidence": "high"
}

字段规则：
- answer：字符串，概括登录流程。
- steps：数组，每一项必须包含 layer、description、evidence。
- layer：只能是 Controller、Service、Mapper、数据库。
- missingInfo：数组；没有缺失信息时返回空数组。
- confidence：只能是 high、medium、low。
- 如果 Controller、Service、Mapper、数据库中某一层资料缺失，也要在 steps 中保留这一层，并在 description 和 evidence 中写“资料中未提供”。

项目资料：
......

用户问题：
分析这个项目的登录流程。
```

**一个合格的输出：**

```
{
  "answer": "资料中未完整提供登录流程，只能确认部分链路信息。",
  "steps": [
    {
      "layer": "Controller",
      "description": "资料中未提供",
      "evidence": "资料中未提供"
    },
    {
      "layer": "Service",
      "description": "资料中未提供",
      "evidence": "资料中未提供"
    },
    {
      "layer": "Mapper",
      "description": "资料中未提供",
      "evidence": "资料中未提供"
    },
    {
      "layer": "数据库",
      "description": "资料中未提供",
      "evidence": "资料中未提供"
    }
  ],
  "missingInfo": ["Controller 层信息", "Service 层信息", "Mapper 层信息", "数据库表信息"],
  "confidence": "low"
}
```

==结构化输出=prompt约束+程序校验+失败处理==



## 20.few-shot

**给模型几个示例，让它模仿格式和思路**





